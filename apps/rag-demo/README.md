# RAG Demo

This application demonstrates Retrieval-Augmented Generation using the `ai-universal-runtime` core framework.

## Setup
1. Configure your `.env` file.
2. Run `npm run ingest` to index your data.
3. Run `npm run query -- "your question"` to query the data.
